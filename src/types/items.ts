export interface DBPantryItem {
  id: number;
  name: string;
  quantity: string;
  location: string;
  email: string;
}

export interface PantryItem {
  id: number;
  name: string;
  quantity: string;
  location: string;
}
